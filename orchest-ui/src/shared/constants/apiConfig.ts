export const HTTP_CLIENT_CONFIG = {
  DEFAULT_TIMEOUT: 60000,
  MAX_RETRIES: 3,
  RETRY_DELAY: 1000,
  RETRYABLE_STATUS_CODES: [408, 429, 500, 502, 503, 504],
} as const;

export const QUERY_CONFIG = {
  RETRY_ATTEMPTS: 2,
  RETRY_DELAY: 1000,
  STALE_TIME: 30000,
  CACHE_TIME: 300000,
  MAX_CONCURRENT_REQUESTS: 5,
} as const;

export const ERROR_HANDLER_CONFIG = {
  MAX_ERROR_QUEUE_SIZE: 100,
  MAX_TOAST_REPETITIONS: 3,
  ERROR_COUNT_RESET_WINDOW: 5 * 60 * 1000,
} as const;

export const PAGINATION_CONFIG = {
  DEFAULT_PAGE_SIZE: 10,
  DEFAULT_PAGE_NUMBER: 0,
  MAX_PAGE_SIZE: 1000,
} as const;
