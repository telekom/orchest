package io.telekom.orchest.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for the Housekeeping module. Enables Spring task scheduling to run periodic jobs
 * (like data purging).
 */
@Configuration
public class HousekeepingConfiguration {}
