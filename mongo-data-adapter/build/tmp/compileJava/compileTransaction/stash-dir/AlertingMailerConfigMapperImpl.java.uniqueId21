package io.telekom.orchest.adapter.mongo.mapper;

import io.telekom.orchest.adapter.mongo.model.AlertingMailerConfig;
import javax.annotation.processing.Generated;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

@Generated(
    value = "org.mapstruct.ap.MappingProcessor",
    date = "2026-08-24T17:08:50+0530",
    comments = "version: 1.5.5.Final, compiler: IncrementalProcessingEnvironment from gradle-language-java-8.14.jar, environment: Java 21.0.4 (Azul Systems, Inc.)"
)
@Component
public class AlertingMailerConfigMapperImpl implements AlertingMailerConfigMapper {

    @Autowired
    private AlertMapper alertMapper;

    @Override
    public AlertingMailerConfig toDocument(io.telekom.orchest.api.core.adapters.data.model.AlertingMailerConfig domain) {
        if ( domain == null ) {
            return null;
        }

        AlertingMailerConfig.AlertingMailerConfigBuilder alertingMailerConfig = AlertingMailerConfig.builder();

        alertingMailerConfig.id( domain.getId() );
        alertingMailerConfig.processId( domain.getProcessId() );
        alertingMailerConfig.alertingRecipient( alertMapper.toDocument( domain.getAlertingRecipient() ) );
        alertingMailerConfig.createdAt( domain.getCreatedAt() );
        alertingMailerConfig.lastModifiedAt( domain.getLastModifiedAt() );

        return alertingMailerConfig.build();
    }

    @Override
    public io.telekom.orchest.api.core.adapters.data.model.AlertingMailerConfig toDomain(AlertingMailerConfig document) {
        if ( document == null ) {
            return null;
        }

        io.telekom.orchest.api.core.adapters.data.model.AlertingMailerConfig.AlertingMailerConfigBuilder alertingMailerConfig = io.telekom.orchest.api.core.adapters.data.model.AlertingMailerConfig.builder();

        alertingMailerConfig.id( document.getId() );
        alertingMailerConfig.processId( document.getProcessId() );
        alertingMailerConfig.alertingRecipient( alertMapper.toDomain( document.getAlertingRecipient() ) );
        alertingMailerConfig.createdAt( document.getCreatedAt() );
        alertingMailerConfig.lastModifiedAt( document.getLastModifiedAt() );

        return alertingMailerConfig.build();
    }
}
